# Some distros (not most) automatically set USERNAME, a redundant copy of USER.
# Since this is non-standard, unset it:
unset USERNAME

# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=
